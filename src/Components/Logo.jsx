import React from 'react';
import logo from "../assets/logo.png"

const Logo = () => {
    return (
        <div>
            <img className='w-[120px]' src={logo} alt="" />
        </div>
    );
};

export default Logo;